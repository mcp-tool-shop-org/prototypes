<?xml version="1.0" encoding="UTF-8" ?>
<UserControl
    x:Class="DevOpTyper.Panels.ExplanationPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Visibility="Collapsed"
    IsTabStop="False"
    AutomationProperties.Name="Snippet perspectives">

    <Border Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
            CornerRadius="8"
            Padding="12"
            Margin="0,0,0,8">
        <StackPanel Spacing="6">
            <!-- Expand/collapse toggle — reachable by keyboard but low tab priority -->
            <ToggleButton x:Name="ExpandToggle"
                          Content="Perspectives"
                          FontSize="11"
                          Padding="6,3"
                          MinWidth="0"
                          IsChecked="False"
                          TabIndex="200"
                          AutomationProperties.Name="Show or hide snippet perspectives"
                          ToolTipService.ToolTip="Expand to see explanatory perspectives for this snippet"/>

            <!-- Content area (collapsed by default, non-interrupting) -->
            <StackPanel x:Name="ContentArea"
                        Spacing="8"
                        Visibility="Collapsed"
                        AutomationProperties.LiveSetting="Off">

                <!-- All perspectives rendered here (including legacy Explain merged as "Notes") -->
                <ItemsControl x:Name="PerspectivesList"
                              IsTabStop="False"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
