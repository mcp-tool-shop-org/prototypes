<Page
    x:Class="NextLedger.App.Views.Spending.SpendingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" RowDefinitions="Auto,Auto,Auto,*" RowSpacing="12" MaxWidth="1200" HorizontalAlignment="Center">
        <StackPanel Orientation="Horizontal" Spacing="12">
            <TextBlock Text="Spending" FontSize="28" FontWeight="SemiBold" />
            <TextBlock Text="{Binding YearMonthText}" FontSize="14" Opacity="0.6" VerticalAlignment="Bottom" Margin="0,0,0,4" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8">
            <Button Content="Refresh" Command="{Binding RefreshCommand}" />
        </StackPanel>

        <!-- Overspent Warning with action hint -->
        <InfoBar Grid.Row="2"
                 IsOpen="{Binding HasOverspentEnvelopes}"
                 IsClosable="False"
                 Severity="Warning"
                 Title="Overspending detected"
                 Message="{Binding OverspentSummary}" />

        <StackPanel Grid.Row="3" Spacing="8">
            <ProgressBar IsIndeterminate="True" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}" />

            <TextBlock Text="{Binding ErrorText}" Foreground="Tomato" TextWrapping="Wrap"
                       Visibility="{Binding ErrorText, Converter={StaticResource StringToVisibilityConverter}}" />

            <InfoBar IsOpen="True"
                    IsClosable="False"
                    Severity="Informational"
                    Title="No spending data yet"
                    Message="Add transactions (or import a CSV) to see spending by envelope."
                    Visibility="{Binding Rows, Converter={StaticResource CollectionEmptyToVisibilityConverter}}" />

            <Grid ColumnDefinitions="*,110,140" ColumnSpacing="12" Padding="4" Opacity="0.8"
                  Visibility="{Binding Rows, Converter={StaticResource CollectionNotEmptyToVisibilityConverter}}">
                <TextBlock Text="Envelope" FontWeight="SemiBold" />
                <TextBlock Grid.Column="1" Text="Spent" FontWeight="SemiBold" HorizontalAlignment="Right" />
                <TextBlock Grid.Column="2" Text="Available" FontWeight="SemiBold" HorizontalAlignment="Right" />
            </Grid>

            <ListView ItemsSource="{Binding Rows}">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0,1" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid ColumnDefinitions="*,110,140" ColumnSpacing="12" Padding="12,8"
                              Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                              CornerRadius="6">
                            <StackPanel Spacing="4">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                    <!-- Overspent indicator with magnitude -->
                                    <FontIcon Glyph="&#xE7BA;" FontSize="12" Foreground="Tomato"
                                              Visibility="{Binding IsOverspent, Converter={StaticResource BoolToVisibilityConverter}}"
                                              ToolTipService.ToolTip="{Binding OverspentHint}" />
                                </StackPanel>
                                <TextBlock Text="{Binding GroupName}" FontSize="11" Opacity="0.5"
                                           Visibility="{Binding GroupName, Converter={StaticResource StringToVisibilityConverter}}" />
                                <ProgressBar Minimum="0" Maximum="100" Value="{Binding SpentPercent}" Height="6" />
                            </StackPanel>

                            <TextBlock Grid.Column="1" Text="{Binding SpentText}" HorizontalAlignment="Right" VerticalAlignment="Center" />

                            <StackPanel Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center" Spacing="2">
                                <TextBlock Text="{Binding AvailableText}"
                                           HorizontalAlignment="Right"
                                           FontWeight="SemiBold"
                                           Foreground="{Binding IsOverspent, Converter={StaticResource OverspentToBrushConverter}}" />
                                <!-- Overspent magnitude hint -->
                                <TextBlock Text="{Binding OverspentHint}" FontSize="10" Foreground="Tomato"
                                           HorizontalAlignment="Right"
                                           Visibility="{Binding IsOverspent, Converter={StaticResource BoolToVisibilityConverter}}" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Page>
