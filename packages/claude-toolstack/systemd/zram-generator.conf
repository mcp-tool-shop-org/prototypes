# /etc/systemd/zram-generator.conf
# Compressed RAM-backed swap — reduces disk I/O during memory pressure
# Ubuntu: apt install systemd-zram-generator
# Fedora: swap-on-zram is typically enabled by default

[zram0]
zram-size = ram / 4
compression-algorithm = zstd
swap-priority = 100
