<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ScalarScope.ViewModels"
             x:Class="ScalarScope.Views.SettingsPage"
             x:DataType="vm:SettingsViewModel"
             Title="Settings"
             BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#0f0f1a}">

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="24" MaximumWidthRequest="700">

            <!-- Header -->
            <Label Text="Settings"
                   FontSize="28"
                   FontAttributes="Bold"
                   TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}"
                   SemanticProperties.HeadingLevel="Level1" />

            <!-- Appearance Section -->
            <Border BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#1a1a2e}"
                    Stroke="{AppThemeBinding Light=#DDDDDD, Dark=#2a2a4e}"
                    StrokeShape="RoundRectangle 12"
                    Padding="20">
                <VerticalStackLayout Spacing="16">
                    <Label Text="Appearance"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="#00d9ff"
                           SemanticProperties.HeadingLevel="Level2" />

                    <!-- Theme -->
                    <Grid ColumnDefinitions="*,Auto" RowSpacing="4">
                        <VerticalStackLayout>
                            <Label Text="Theme"
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}"
                                   SemanticProperties.Description="Select application color theme" />
                            <Label Text="Choose light, dark, or match system settings"
                                   FontSize="12"
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#888888}" />
                        </VerticalStackLayout>
                        <Picker Grid.Column="1"
                                ItemsSource="{Binding ThemeOptions}"
                                SelectedIndex="{Binding ThemeIndex}"
                                WidthRequest="140"
                                SemanticProperties.Description="Theme selection dropdown" />
                    </Grid>

                    <BoxView HeightRequest="1" Color="{AppThemeBinding Light=#EEEEEE, Dark=#2a2a4e}" />

                    <!-- High Contrast -->
                    <Grid ColumnDefinitions="*,Auto">
                        <VerticalStackLayout>
                            <Label Text="High Contrast Mode"
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}" />
                            <Label Text="Increase color contrast for better visibility"
                                   FontSize="12"
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#888888}" />
                        </VerticalStackLayout>
                        <Switch Grid.Column="1"
                                IsToggled="{Binding HighContrastMode}"
                                OnColor="#00d9ff"
                                SemanticProperties.Description="Toggle high contrast mode" />
                    </Grid>

                    <BoxView HeightRequest="1" Color="{AppThemeBinding Light=#EEEEEE, Dark=#2a2a4e}" />

                    <!-- Reduce Animations -->
                    <Grid ColumnDefinitions="*,Auto">
                        <VerticalStackLayout>
                            <Label Text="Reduce Animations"
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}" />
                            <Label Text="Minimize motion for accessibility"
                                   FontSize="12"
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#888888}" />
                        </VerticalStackLayout>
                        <Switch Grid.Column="1"
                                IsToggled="{Binding ReduceAnimations}"
                                OnColor="#00d9ff"
                                SemanticProperties.Description="Toggle reduced animations" />
                    </Grid>

                    <BoxView HeightRequest="1" Color="{AppThemeBinding Light=#EEEEEE, Dark=#2a2a4e}" />

                    <!-- Annotation Density -->
                    <Grid ColumnDefinitions="*,Auto" RowSpacing="4">
                        <VerticalStackLayout>
                            <Label Text="Annotation Density"
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}" />
                            <Label Text="Control amount of on-screen annotations"
                                   FontSize="12"
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#888888}" />
                        </VerticalStackLayout>
                        <Picker Grid.Column="1"
                                ItemsSource="{Binding AnnotationOptions}"
                                SelectedIndex="{Binding AnnotationDensityIndex}"
                                WidthRequest="140"
                                SemanticProperties.Description="Annotation density selection" />
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <!-- Accessibility Section (Phase 4.3) -->
            <Border BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#1a1a2e}"
                    Stroke="{AppThemeBinding Light=#DDDDDD, Dark=#2a2a4e}"
                    StrokeShape="RoundRectangle 12"
                    Padding="20">
                <VerticalStackLayout Spacing="16">
                    <Label Text="Accessibility"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="#c56cf0"
                           SemanticProperties.HeadingLevel="Level2" />

                    <!-- Color Vision Mode -->
                    <Grid ColumnDefinitions="*,Auto" RowSpacing="4">
                        <VerticalStackLayout>
                            <Label Text="Color Vision Mode"
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}"
                                   SemanticProperties.Description="Select color palette for colorblind users" />
                            <Label Text="Optimized palettes for different types of color vision"
                                   FontSize="12"
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#888888}" />
                        </VerticalStackLayout>
                        <Picker Grid.Column="1"
                                ItemsSource="{Binding ColorVisionOptions}"
                                SelectedIndex="{Binding ColorVisionIndex}"
                                WidthRequest="160"
                                SemanticProperties.Description="Color vision mode selection" />
                    </Grid>

                    <BoxView HeightRequest="1" Color="{AppThemeBinding Light=#EEEEEE, Dark=#2a2a4e}" />

                    <!-- Screen Reader Support -->
                    <Grid ColumnDefinitions="*,Auto">
                        <VerticalStackLayout>
                            <Label Text="Enhanced Screen Reader"
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}" />
                            <Label Text="Generate detailed audio descriptions of visualizations"
                                   FontSize="12"
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#888888}" />
                        </VerticalStackLayout>
                        <Switch Grid.Column="1"
                                IsToggled="{Binding ScreenReaderMode}"
                                OnColor="#c56cf0"
                                SemanticProperties.Description="Toggle enhanced screen reader mode" />
                    </Grid>

                    <BoxView HeightRequest="1" Color="{AppThemeBinding Light=#EEEEEE, Dark=#2a2a4e}" />

                    <!-- Large Pointer -->
                    <Grid ColumnDefinitions="*,Auto">
                        <VerticalStackLayout>
                            <Label Text="Large Pointer"
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}" />
                            <Label Text="Increase size of selection indicators and cursors"
                                   FontSize="12"
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#888888}" />
                        </VerticalStackLayout>
                        <Switch Grid.Column="1"
                                IsToggled="{Binding LargePointer}"
                                OnColor="#c56cf0"
                                SemanticProperties.Description="Toggle large pointer mode" />
                    </Grid>

                    <BoxView HeightRequest="1" Color="{AppThemeBinding Light=#EEEEEE, Dark=#2a2a4e}" />

                    <!-- Text Scale -->
                    <VerticalStackLayout Spacing="8">
                        <Grid ColumnDefinitions="*,Auto">
                            <Label Text="Text Scale"
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}" />
                            <Label Grid.Column="1"
                                   Text="{Binding TextScaleDisplay}"
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#888888}"
                                   WidthRequest="50"
                                   HorizontalTextAlignment="End" />
                        </Grid>
                        <Slider Minimum="0.75"
                                Maximum="2.0"
                                Value="{Binding TextScale}"
                                ThumbColor="#c56cf0"
                                MinimumTrackColor="#c56cf0"
                                MaximumTrackColor="{AppThemeBinding Light=#DDDDDD, Dark=#2a2a4e}"
                                SemanticProperties.Description="Text scale slider" />
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </Border>

            <!-- Playback Section -->
            <Border BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#1a1a2e}"
                    Stroke="{AppThemeBinding Light=#DDDDDD, Dark=#2a2a4e}"
                    StrokeShape="RoundRectangle 12"
                    Padding="20">
                <VerticalStackLayout Spacing="16">
                    <Label Text="Playback"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="#4ecdc4"
                           SemanticProperties.HeadingLevel="Level2" />

                    <!-- Default Speed -->
                    <Grid ColumnDefinitions="*,Auto" RowSpacing="4">
                        <VerticalStackLayout>
                            <Label Text="Default Playback Speed"
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}" />
                            <Label Text="Speed when a new run is loaded"
                                   FontSize="12"
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#888888}" />
                        </VerticalStackLayout>
                        <Picker Grid.Column="1"
                                ItemsSource="{Binding SpeedOptions}"
                                SelectedIndex="{Binding DefaultSpeedIndex}"
                                WidthRequest="140"
                                SemanticProperties.Description="Default playback speed selection" />
                    </Grid>

                    <BoxView HeightRequest="1" Color="{AppThemeBinding Light=#EEEEEE, Dark=#2a2a4e}" />

                    <!-- Auto-play -->
                    <Grid ColumnDefinitions="*,Auto">
                        <VerticalStackLayout>
                            <Label Text="Auto-Play on Load"
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}" />
                            <Label Text="Start playback automatically when loading a run"
                                   FontSize="12"
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#888888}" />
                        </VerticalStackLayout>
                        <Switch Grid.Column="1"
                                IsToggled="{Binding AutoPlayOnLoad}"
                                OnColor="#4ecdc4"
                                SemanticProperties.Description="Toggle auto-play on load" />
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <!-- Session Section -->
            <Border BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#1a1a2e}"
                    Stroke="{AppThemeBinding Light=#DDDDDD, Dark=#2a2a4e}"
                    StrokeShape="RoundRectangle 12"
                    Padding="20">
                <VerticalStackLayout Spacing="16">
                    <Label Text="Session"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="#a29bfe"
                           SemanticProperties.HeadingLevel="Level2" />

                    <!-- Auto-load Last Session -->
                    <Grid ColumnDefinitions="*,Auto">
                        <VerticalStackLayout>
                            <Label Text="Auto-Load Last Session"
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}" />
                            <Label Text="Restore previous run on startup"
                                   FontSize="12"
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#888888}" />
                        </VerticalStackLayout>
                        <Switch Grid.Column="1"
                                IsToggled="{Binding AutoLoadLastSession}"
                                OnColor="#a29bfe"
                                SemanticProperties.Description="Toggle auto-load last session" />
                    </Grid>

                    <BoxView HeightRequest="1" Color="{AppThemeBinding Light=#EEEEEE, Dark=#2a2a4e}" />

                    <!-- Recent Files Limit -->
                    <Grid ColumnDefinitions="*,Auto" RowSpacing="4">
                        <VerticalStackLayout>
                            <Label Text="Recent Files Limit"
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}" />
                            <Label Text="Maximum files to remember"
                                   FontSize="12"
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#888888}" />
                        </VerticalStackLayout>
                        <Picker Grid.Column="1"
                                ItemsSource="{Binding RecentFilesOptions}"
                                SelectedIndex="{Binding RecentFilesLimitIndex}"
                                WidthRequest="140"
                                SemanticProperties.Description="Recent files limit selection" />
                    </Grid>

                    <BoxView HeightRequest="1" Color="{AppThemeBinding Light=#EEEEEE, Dark=#2a2a4e}" />

                    <!-- Clear Recent Files -->
                    <Grid ColumnDefinitions="*,Auto">
                        <VerticalStackLayout>
                            <Label Text="Recent Files"
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}" />
                            <Label Text="Clear the recent files list"
                                   FontSize="12"
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#888888}" />
                        </VerticalStackLayout>
                        <Button Grid.Column="1"
                                Text="Clear"
                                Command="{Binding ClearRecentFilesCommand}"
                                BackgroundColor="{AppThemeBinding Light=#DDDDDD, Dark=#2a2a4e}"
                                TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}"
                                Padding="16,8"
                                CornerRadius="6"
                                SemanticProperties.Description="Clear recent files list" />
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <!-- Export Section -->
            <Border BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#1a1a2e}"
                    Stroke="{AppThemeBinding Light=#DDDDDD, Dark=#2a2a4e}"
                    StrokeShape="RoundRectangle 12"
                    Padding="20">
                <VerticalStackLayout Spacing="16">
                    <Label Text="Export"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="#ff9f43"
                           SemanticProperties.HeadingLevel="Level2" />

                    <!-- Default Export Path -->
                    <VerticalStackLayout Spacing="8">
                        <Label Text="Default Export Folder"
                               FontAttributes="Bold"
                               TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}" />
                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
                            <Entry Text="{Binding DefaultExportPath}"
                                   Placeholder="Choose folder..."
                                   IsReadOnly="True"
                                   BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#16213e}"
                                   TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}"
                                   SemanticProperties.Description="Default export folder path" />
                            <Button Grid.Column="1"
                                    Text="Browse..."
                                    Command="{Binding BrowseExportPathCommand}"
                                    BackgroundColor="#ff9f43"
                                    TextColor="White"
                                    Padding="16,8"
                                    CornerRadius="6"
                                    SemanticProperties.Description="Browse for export folder" />
                        </Grid>
                    </VerticalStackLayout>

                    <BoxView HeightRequest="1" Color="{AppThemeBinding Light=#EEEEEE, Dark=#2a2a4e}" />

                    <!-- Default Resolution -->
                    <VerticalStackLayout Spacing="8">
                        <Label Text="Default Export Resolution"
                               FontAttributes="Bold"
                               TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}" />
                        <HorizontalStackLayout Spacing="10">
                            <Entry Text="{Binding DefaultExportWidth}"
                                   Keyboard="Numeric"
                                   WidthRequest="80"
                                   BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#16213e}"
                                   TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}"
                                   SemanticProperties.Description="Export width in pixels" />
                            <Label Text="×"
                                   VerticalOptions="Center"
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#888888}" />
                            <Entry Text="{Binding DefaultExportHeight}"
                                   Keyboard="Numeric"
                                   WidthRequest="80"
                                   BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#16213e}"
                                   TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}"
                                   SemanticProperties.Description="Export height in pixels" />
                            <Label Text="pixels"
                                   VerticalOptions="Center"
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#888888}" />
                        </HorizontalStackLayout>
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </Border>

            <!-- Advanced Section -->
            <Border BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#1a1a2e}"
                    Stroke="{AppThemeBinding Light=#DDDDDD, Dark=#2a2a4e}"
                    StrokeShape="RoundRectangle 12"
                    Padding="20">
                <VerticalStackLayout Spacing="16">
                    <Label Text="Advanced"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="#ff6b6b"
                           SemanticProperties.HeadingLevel="Level2" />

                    <!-- Reset Demo -->
                    <Grid ColumnDefinitions="*,Auto">
                        <VerticalStackLayout>
                            <Label Text="First-Run Experience"
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}" />
                            <Label Text="Show the welcome demo again"
                                   FontSize="12"
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#888888}" />
                        </VerticalStackLayout>
                        <Button Grid.Column="1"
                                Text="Reset"
                                Command="{Binding ResetDemoStateCommand}"
                                BackgroundColor="{AppThemeBinding Light=#DDDDDD, Dark=#2a2a4e}"
                                TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}"
                                Padding="16,8"
                                CornerRadius="6"
                                SemanticProperties.Description="Reset first-run demo state" />
                    </Grid>

                    <BoxView HeightRequest="1" Color="{AppThemeBinding Light=#EEEEEE, Dark=#2a2a4e}" />

                    <!-- Reset All -->
                    <Grid ColumnDefinitions="*,Auto">
                        <VerticalStackLayout>
                            <Label Text="Reset All Settings"
                                   FontAttributes="Bold"
                                   TextColor="#ff6b6b" />
                            <Label Text="Restore all settings to defaults"
                                   FontSize="12"
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#888888}" />
                        </VerticalStackLayout>
                        <Button Grid.Column="1"
                                Text="Reset All"
                                Command="{Binding ResetAllSettingsCommand}"
                                BackgroundColor="#ff6b6b"
                                TextColor="White"
                                Padding="16,8"
                                CornerRadius="6"
                                SemanticProperties.Description="Reset all settings to defaults" />
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <!-- About Section -->
            <Border BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#1a1a2e}"
                    Stroke="{AppThemeBinding Light=#DDDDDD, Dark=#2a2a4e}"
                    StrokeShape="RoundRectangle 12"
                    Padding="20">
                <VerticalStackLayout Spacing="16">
                    <Label Text="About ScalarScope"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="#00d9ff"
                           SemanticProperties.HeadingLevel="Level2" />

                    <!-- Version Info -->
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="16" RowDefinitions="Auto,Auto,Auto,Auto" RowSpacing="12">
                        <Label Grid.Row="0" Grid.Column="0" 
                               Text="Version" 
                               FontSize="13"
                               TextColor="{AppThemeBinding Light=#666666, Dark=#888888}"/>
                        <Label Grid.Row="0" Grid.Column="1" 
                               Text="2.0.0" 
                               FontSize="13"
                               FontAttributes="Bold"
                               TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}"/>
                        
                        <Label Grid.Row="1" Grid.Column="0" 
                               Text="DeltaSpec" 
                               FontSize="13"
                               TextColor="{AppThemeBinding Light=#666666, Dark=#888888}"/>
                        <Label Grid.Row="1" Grid.Column="1" 
                               Text="1.0.0" 
                               FontSize="13"
                               FontFamily="Consolas"
                               TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}"/>
                        
                        <Label Grid.Row="2" Grid.Column="0" 
                               Text="Platform" 
                               FontSize="13"
                               TextColor="{AppThemeBinding Light=#666666, Dark=#888888}"/>
                        <Label Grid.Row="2" Grid.Column="1" 
                               Text="Windows 10+" 
                               FontSize="13"
                               TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}"/>
                        
                        <Label Grid.Row="3" Grid.Column="0" 
                               Text="Store ID" 
                               FontSize="13"
                               TextColor="{AppThemeBinding Light=#666666, Dark=#888888}"/>
                        <Label Grid.Row="3" Grid.Column="1" 
                               Text="9P3HT1PHBKQK" 
                               FontSize="13"
                               FontFamily="Consolas"
                               TextColor="{AppThemeBinding Light=#666666, Dark=#888888}"/>
                    </Grid>

                    <BoxView HeightRequest="1" Color="{AppThemeBinding Light=#EEEEEE, Dark=#2a2a4e}" />

                    <!-- Bundle Hash Explanation -->
                    <VerticalStackLayout Spacing="8">
                        <Label Text="What's a Bundle Hash?"
                               FontAttributes="Bold"
                               FontSize="13"
                               TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}"/>
                        <Label Text="When you export a comparison, ScalarScope creates a unique cryptographic hash (e.g., SHA-256). This hash acts as a fingerprint—if anyone modifies the bundle, the hash won't match, ensuring reproducibility and audit integrity."
                               FontSize="12"
                               TextColor="{AppThemeBinding Light=#666666, Dark=#888888}"
                               LineBreakMode="WordWrap"/>
                    </VerticalStackLayout>

                    <BoxView HeightRequest="1" Color="{AppThemeBinding Light=#EEEEEE, Dark=#2a2a4e}" />

                    <!-- Privacy/Telemetry Statement -->
                    <VerticalStackLayout Spacing="8">
                        <HorizontalStackLayout Spacing="8">
                            <Label Text="●" FontSize="8" TextColor="#00ff88" VerticalOptions="Center"/>
                            <Label Text="Privacy"
                                   FontAttributes="Bold"
                                   FontSize="13"
                                   TextColor="#00ff88"/>
                        </HorizontalStackLayout>
                        <Label Text="ScalarScope collects no telemetry, sends no analytics, and stores all your data locally. Your inference runs and comparisons never leave your machine unless you explicitly export them."
                               FontSize="12"
                               TextColor="{AppThemeBinding Light=#666666, Dark=#888888}"
                               LineBreakMode="WordWrap"/>
                    </VerticalStackLayout>

                    <BoxView HeightRequest="1" Color="{AppThemeBinding Light=#EEEEEE, Dark=#2a2a4e}" />

                    <!-- Links -->
                    <HorizontalStackLayout Spacing="20">
                        <Label Text="Documentation"
                               FontSize="12"
                               TextColor="#00d9ff"
                               TextDecorations="Underline">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding OpenDocumentationCommand}"/>
                            </Label.GestureRecognizers>
                        </Label>
                        <Label Text="GitHub"
                               FontSize="12"
                               TextColor="#00d9ff"
                               TextDecorations="Underline">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding OpenGitHubCommand}"/>
                            </Label.GestureRecognizers>
                        </Label>
                        <Label Text="Report Issue"
                               FontSize="12"
                               TextColor="#00d9ff"
                               TextDecorations="Underline">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding OpenIssueCommand}"/>
                            </Label.GestureRecognizers>
                        </Label>
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Border>

            <!-- Footer -->
            <VerticalStackLayout Spacing="4" HorizontalOptions="Center" Margin="0,10,0,20">
                <Label Text="© 2026 MCP Tool Shop"
                       FontSize="11"
                       TextColor="{AppThemeBinding Light=#999999, Dark=#666666}"
                       HorizontalOptions="Center"/>
                <Label Text="Built with scientific rigor"
                       FontSize="11"
                       TextColor="{AppThemeBinding Light=#BBBBBB, Dark=#444444}"
                       HorizontalOptions="Center"/>
            </VerticalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
