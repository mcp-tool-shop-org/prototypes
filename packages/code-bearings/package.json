{
  "private": true,
  "name": "code-bearings-workspace",
  "version": "1.0.0",
  "description": "Code Bearings monorepo — source-grounded control for modern codebases",
  "workspaces": [
    "packages/core",
    "packages/cli",
    "packages/vscode"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspace=packages/core",
    "lint": "npm run lint --workspaces",
    "verify": "npm run build && npm run lint && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
