<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Attestia.App.Pages.CompliancePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Loaded="Page_Loaded">

    <ScrollViewer>
        <StackPanel Padding="32,28,32,32" Spacing="20" MaxWidth="1100">
            <!-- Header -->
            <StackPanel Spacing="4">
                <TextBlock Text="Compliance" Style="{StaticResource AttestiaPageTitleStyle}" />
                <TextBlock Text="Generate audit-ready reports backed by ledger proofs."
                           FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>

            <!-- Framework selector -->
            <StackPanel Spacing="8">
                <TextBlock Text="Framework" Style="{StaticResource AttestiaSectionHeadingStyle}" />
                <TextBlock Text="Why this matters: Compliance reports map your attestation evidence to specific regulatory controls. Each control is evaluated against cryptographic proofs from your ledger, creating audit-ready documentation."
                           FontSize="11" Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                           TextWrapping="Wrap" FontStyle="Italic" LineHeight="16" />
                <Grid Padding="20" CornerRadius="8"
                      Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                      BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                      BorderThickness="1">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Select a compliance framework to evaluate your attestation records against."
                                   FontSize="13" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <ComboBox x:Name="FrameworkCombo" PlaceholderText="Select framework"
                                  Width="300" SelectionChanged="Framework_Changed" />
                        <Button Content="Generate Attested Report" Click="GenerateReport_Click" />
                    </StackPanel>
                </Grid>
            </StackPanel>

            <!-- Empty state (no frameworks loaded) -->
            <Grid x:Name="EmptyState" Visibility="Collapsed"
                  HorizontalAlignment="Center" Padding="0,16,0,0">
                <StackPanel Spacing="12" MaxWidth="420" HorizontalAlignment="Center">
                    <FontIcon Glyph="&#xE73E;" FontSize="48"
                              Foreground="{ThemeResource TextFillColorTertiaryBrush}" HorizontalAlignment="Center" />
                    <TextBlock Text="No compliance frameworks available" FontSize="18" FontWeight="SemiBold"
                               HorizontalAlignment="Center" />
                    <TextBlock TextWrapping="Wrap" TextAlignment="Center" FontSize="13" LineHeight="20"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               Text="Compliance frameworks define regulatory controls and map them to attestation evidence. Ensure the attestation engine is running and has frameworks configured." />
                </StackPanel>
            </Grid>

            <!-- Report summary -->
            <StackPanel x:Name="ReportPanel" Visibility="Collapsed" Spacing="8">
                <TextBlock Text="Report" Style="{StaticResource AttestiaSectionHeadingStyle}" />

                <!-- Score card -->
                <Grid Padding="20" CornerRadius="8"
                      Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                      BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                      BorderThickness="1" ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Spacing="4">
                        <TextBlock Text="Score" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock x:Name="ScoreText" Text="—" FontSize="32" Style="{StaticResource AttestiaNumericStyle}" HorizontalTextAlignment="Left" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" Spacing="4">
                        <TextBlock Text="Passed" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock x:Name="PassedText" Text="—" FontSize="32" Style="{StaticResource AttestiaNumericStyle}" HorizontalTextAlignment="Left" />
                    </StackPanel>

                    <StackPanel Grid.Column="2" Spacing="4">
                        <TextBlock Text="Total Controls" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock x:Name="TotalText" Text="—" FontSize="32" Style="{StaticResource AttestiaNumericStyle}" HorizontalTextAlignment="Left" />
                    </StackPanel>
                </Grid>

                <!-- Control evaluations -->
                <ListView x:Name="EvaluationsList" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Padding="16" Margin="0,0,0,6"
                                  CornerRadius="8"
                                  Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                  BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                  BorderThickness="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0"
                                           Text="{Binding Mapping.ControlId}" FontWeight="SemiBold" />
                                <Border Grid.Row="0" Grid.Column="1" Padding="6,2" CornerRadius="4"
                                        Background="{ThemeResource SubtleFillColorSecondaryBrush}">
                                    <TextBlock Text="{Binding Mapping.Status}" FontSize="11" />
                                </Border>

                                <TextBlock Grid.Row="1" Grid.ColumnSpan="2" Margin="0,4,0,0"
                                           Text="{Binding Mapping.ControlName}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

                                <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal"
                                            Spacing="8" Margin="0,4,0,0">
                                    <TextBlock FontSize="11" FontWeight="SemiBold"
                                               Text="{Binding Passed}" />
                                    <TextBlock FontSize="11" Text="{Binding EvidenceDetail}"
                                               TextTrimming="CharacterEllipsis"
                                               Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>

            <!-- Loading / Error -->
            <StackPanel Orientation="Horizontal" Spacing="8">
                <ProgressRing x:Name="LoadingRing" IsActive="False" Width="20" Height="20" />
                <TextBlock x:Name="ErrorText" FontSize="13"
                           Foreground="{ThemeResource SystemFillColorCautionBrush}" VerticalAlignment="Center" />
                <Button x:Name="RetryBtn" Content="Reattempt Connection" Click="GenerateReport_Click"
                        Visibility="Collapsed" FontSize="12" Padding="8,4" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
