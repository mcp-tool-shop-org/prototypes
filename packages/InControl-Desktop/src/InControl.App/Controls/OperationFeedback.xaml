<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="InControl.App.Controls.OperationFeedback"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
    OperationFeedback: Inline notification for operation results.
    Shows success/error/warning/info messages with auto-dismiss.
    Use for: Copy confirmation, Save complete, Validation errors, etc.
    -->
    <Border x:Name="FeedbackBorder"
            CornerRadius="{StaticResource RadiusSM}"
            Padding="12,8"
            Visibility="Collapsed"
            HorizontalAlignment="Left"
            VerticalAlignment="Center">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Status Icon -->
            <FontIcon x:Name="StatusIcon"
                      Grid.Column="0"
                      FontSize="16"
                      VerticalAlignment="Center" />

            <!-- Message -->
            <TextBlock x:Name="MessageText"
                       Grid.Column="1"
                       Margin="8,0,0,0"
                       VerticalAlignment="Center"
                       TextTrimming="CharacterEllipsis" />

            <!-- Dismiss Button (optional) -->
            <Button x:Name="DismissButton"
                    Grid.Column="2"
                    Style="{StaticResource GhostButtonStyle}"
                    Padding="4"
                    Margin="8,0,0,0"
                    Visibility="Collapsed"
                    ToolTipService.ToolTip="Dismiss">
                <FontIcon Glyph="&#xE8BB;" FontSize="12" />
            </Button>
        </Grid>
    </Border>
</UserControl>
