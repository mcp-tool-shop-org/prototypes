# Claude Rules

Dispatch table generator and instruction-file optimizer for Claude Code.

## Stack
- TypeScript ESM, depends on ai-loadout
- CLI commands: analyze, split, validate, stats
- Three layers: CLAUDE.md → index.json → rule files
