# Calibration Corpus Specification — v0.9.0

## Purpose

Calibration content exists to teach the engine, not the user. It provides ground truth: a known set of CodeItems with predictable difficulty bands, allowing the session planner to make intelligent selection choices from day one.

## Target Languages (Initial Set)

1. **Python** — dynamic, significant whitespace, low symbol density
2. **Rust** — static, high symbol density, deep nesting
3. **JavaScript** — curly-brace, moderate density, template literals
4. **C#** — curly-brace, generics, LINQ operators
5. **Go** — minimal syntax, moderate nesting, error handling patterns

Additional languages may be added in future versions.

## Difficulty Band Coverage

Each language pack must cover all 7 difficulty bands with minimum item counts:

| Band | Min Items | Typical Characteristics |
|------|-----------|------------------------|
| D1   | 5         | 1-5 lines, low density, flat |
| D2   | 5         | 6-10 lines, low density, shallow nesting |
| D3   | 5         | 8-15 lines, moderate density, 1-2 indent levels |
| D4   | 5         | 12-20 lines, moderate density, 2-3 indent levels |
| D5   | 5         | 15-25 lines, higher density, 3 indent levels |
| D6   | 5         | 20-30 lines, high density, 3-4 indent levels |
| D7   | 3         | 25+ lines, high density, 4+ indent levels |

Minimum per language: 33 items. Target: 35-50.

## File & Folder Convention

```
assets/calibration/
  python.json
  rust.json
  javascript.json
  csharp.json
  go.json
```

Each file is a JSON array of `Snippet` objects following the existing schema:

```json
[
  {
    "Id": "cal-py-d1-001",
    "Language": "python",
    "Difficulty": 1,
    "Title": "Simple assignment",
    "Code": "x = 42\n",
    "Topics": ["assignment"],
    "Explain": ["Assigns integer 42 to variable x"]
  }
]
```

### Naming Convention

- **Id**: `cal-{lang}-d{band}-{seq}` (e.g., `cal-py-d3-007`)
- **Title**: descriptive, lowercase, no articles

## Calibration vs Ship-to-Users

| Aspect | Calibration | User Content |
|--------|-------------|--------------|
| Purpose | Engine ground truth | Practice material |
| Origin tag | `calibration-pack-v1` | `builtin-pack` or `pack:{lang}` |
| Source tag | `calibration` | `builtin` or `user` |
| Editable | No | User packs are editable |
| Shown in stats | Configurable (default: excluded) | Always included |
| Difficulty | Authored and verified | Authored or derived |
| Selection | Planner-driven | Rating + weakness driven |

## Ingestion

Calibration packs are ingested into the `ContentPipeline` with:
- `source = "calibration"`
- `origin = "calibration-pack-v1"`

The `ContentQuery.Source` filter can include or exclude calibration content.

## Invariants

1. Every band 1-7 has at least the minimum item count per language.
2. Calibration items' derived difficulty (from `DifficultyDeriver`) is within ±2 of the authored band for at least 75% of snippets per language. The deriver is a coarse 3-metric formula — authored difficulty captures conceptual complexity that metrics alone cannot measure. The sidecar metadata preserves authored difficulty for the session planner.
3. Calibration IDs are stable across versions (content-addressed).
4. Calibration content is never modified by user actions.
5. Removing calibration packs does not corrupt the content library.
6. Calibration content is excluded from `GetSnippets()` (practice) and only accessible via `GetCalibrationSnippets()` (planner).

## Implementation Notes (Phase 1)

### Content Pipeline Integration

- `CalibrationAssetProvider` loads JSON packs from `Assets/calibration/` in the output directory
- `ContentPipeline.IngestCalibration()` stores items with source=calibration, origin=calibration-pack-v1
- A `_calibrationIds` HashSet tracks calibration content-addressed IDs for filtering
- `GetSnippets()` excludes calibration items; `GetCalibrationSnippets()` returns calibration-only
- Sidecar metadata preserves authored Difficulty, Topics, and Explain from the JSON packs

### Derivation Gap

The `DifficultyDeriver` uses three bands (lines, symbol density, indent depth) to produce a score 0-9 mapped to difficulty 1-7. This formula captures structural complexity but misses:
- **Conceptual complexity** (algorithms, patterns, ownership semantics)
- **Language-specific idioms** (Rust's borrow checker, Python's generators, C# generics)
- **Reading difficulty** (dense type annotations, deeply nested generics)

The session planner should always use the authored difficulty from sidecar metadata, not the derived value. The derivation tests serve as a canary — if the gap widens significantly with new calibration content, the packs should be reviewed.

### Test Coverage (Phase 1)

| Test File | Tests | Purpose |
|-----------|-------|---------|
| CalibrationPackTests.cs | 52 | Structure, coverage, ID format, uniqueness |
| CalibrationIngestionTests.cs | 13 | Pipeline integration, isolation, sidecar preservation |
| CalibrationDerivationTests.cs | 17 | Derivation invariants, trend, bounded gap |
| SnippetFixtures.cs | — | Factory methods for Phase 2 planner tests |
